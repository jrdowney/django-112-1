# django-112-1

First Django project
Please deliver the first Django project created during class with the following requirements:

- Simple Django project

- With 1 application inside of it 

- Returns a simple string when user visits home page

- Returns your name when user visits /about
